const storage = @import("../storage/publication_storage.zig");

pub const VtSurfaceSlot = storage.VtSurfaceSlot;
pub const RetainedSlot = storage.RetainedSlot;
pub const SourceSlot = storage.SourceSlot;
pub const slotCellCount = storage.slotCellCount;
pub const slotCellCountChecked = storage.slotCellCountChecked;
